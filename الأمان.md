# الأمان — ملخص وتوصيات

## ✅ ما هو آمن حالياً

### الدفع (الباي بال والفيزا)
- **لا يتم إدخال أي بيانات بطاقة على موقعك.** الزائر ينتقل إلى صفحة الباي بال الرسمية للدفع، والمدفوعات تُعالج بالكامل على خوادم الباي بال.
- إيميل الباي بال الظاهر في الكود (في `payment-config.js`) **ليس سراً** — مثل وضع إيميلك على صفحة تواصل. لا يوجد كلمات سر أو مفاتيح API حساسة في الموقع.

### عرض المحتوى (حماية من XSS)
- في لوحة التحكم وعرض المبيعات والكتب يتم **تهريب النص** (escape) قبل إدخاله في الصفحة، فلا يمكن تنفيذ سكربتات ضارة من خلال بيانات المبيعات أو أسماء الكتب.

### الروابط الخارجية
- روابط واتساب وإنستغرام والباي بال تستخدم `target="_blank"` مع `rel="noopener"` حيث وُضعت، مما يقلل مخاطر التلاعب من نافذة الموقع.

---

## ⚠️ نقاط تحتاج انتباهاً

### 1. لوحة التحكم (admin) بدون كلمة سر
- أي شخص يعرف رابط `admin.html` يمكنه فتح لوحة التحكم ورؤية إحصائيات المبيعات والكتب.
- **التوصية:**
  - إن كان الموقع على استضافة تسمح بحماية مجلد (مثلاً كلمة مرور للمجلد): ضعي `admin.html` داخل مجلد وافعلي حماية بكلمة مرور من لوحة الاستضافة.
  - أو غيّري اسم الملف إلى شيء غير متوقع (مثلاً `dashboard-xyz.html`) ولا تشاركيه إلا معكِ — لا يغني عن حماية الخادم لكن يقلل الوصول العشوائي.

### 2. بيانات المبيعات في المتصفح (localStorage)
- بيانات المبيعات مخزنة في **localStorage** في جهاز من يفتح لوحة التحكم. لا تُرسل إلى خادم.
- **النتيجة:** من يفتح اللوحة من جهاز عام أو يشارك جهازه قد يرى أو يغيّر هذه البيانات. ليست بيانات دفع حساسة، لكنها إدارية.
- **لاحقاً:** عند وجود خادم أو قاعدة بيانات، يُفضّل نقل تسجيل المبيعات إلى الخادم بعد التحقق من الدفع عبر الباي بال (مثلاً IPN/Webhooks).

### 3. نموذج التواصل
- حالياً النموذج لا يرسل إلى خادم. عند ربطه بخدمة (مثل Formspree):
  - استخدمي **HTTPS** دائماً لموقعك حتى لا تُرسل النماذج بدون تشفير.
  - Formspree يوفّر حماية أساسية من السبام؛ تفعيل الـ reCAPTCHA (إن وُجد) يزيد الحماية.

### 4. تشفير الاتصال (HTTPS)
- عند رفع الموقع على استضافة، **فعّلي HTTPS** (شهادة SSL) من لوحة التحكم أو من إعدادات النطاق. معظم الاستضافات توفّرها مجاناً.
- بدون HTTPS يمكن اعتراض محتوى الصفحات والنماذج من نفس الشبكة.

---

## 📋 ملخص سريع

| الجانب              | الحالة        | ملاحظة |
|---------------------|---------------|--------|
| بيانات البطاقات    | غير موجودة في الموقع | الدفع على الباي بال فقط |
| إيميل الباي بال     | ظاهر في الكود | مقبول — ليس سراً |
| عرض البيانات (XSS)  | محمي          | استخدام تهريب النص في اللوحة والعروض |
| لوحة التحكم         | بدون كلمة سر  | يُنصح بحماية مجلد أو إخفاء الرابط |
| تخزين المبيعات     | localStorage  | مقبول للتشغيل الحالي؛ خادم لاحقاً أفضل |
| HTTPS               | يعتمد على الاستضافة | يجب تفعيله عند النشر |

---

**الخلاصة:** الموقع من ناحية الدفع وعرض المحتوى آمن للاستخدام العادي. لأمان أفضل: استخدمي HTTPS، واحمي لوحة التحكم (كلمة مرور مجلد أو رابط غير واضح)، وعند الإمكان انقلي تسجيل المبيعات إلى خادم بعد التحقق من الدفع.
