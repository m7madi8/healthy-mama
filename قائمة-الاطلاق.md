# قائمة التحقق قبل إطلاق الموقع

## ✅ ما هو جاهز
- الصفحة الرئيسية (هيرو، من أنا، استبيانات، كتب، تواصل)
- صفحات الكتب الأربعة مع السعر والوصف
- الدفع عبر الباي بال والفيزا/ماستركارد (بعد إعداد الإيميل)
- صفحة شكر بعد الشراء
- لوحة تحكم للإدارة (عرض الكتب والمبيعات)
- التصميم المتجاوب والعربي RTL
- روابط الشريط والتذييل (اللوجو يوجّه للرئيسية)

---

## ⚠️ يجب إكماله قبل الإطلاق

### 1. إيميل الباي بال (ضروري لاستقبال المدفوعات)
- **الملف:** `payment-config.js`
- **الإجراء:** استبدلي `your-email@example.com` بإيميل حساب الباي بال الحقيقي الذي تستقبلين فيه المدفوعات.
- بدون ذلك لن يظهر زر الدفع الحقيقي في صفحات الكتب (سيظهر تنبيه للمطور فقط).

### 2. روابط واتساب وإنستغرام
- **الملف:** `index.html`
- **واتساب:** الرابط الحالي `https://wa.me/972501234567` — غيّريه إلى رقمك مع رمز الدولة (مثلاً 972 لإسرائيل ثم رقم الجوال بدون صفر).
- **إنستغرام:** الرابط الحالي `https://instagram.com` — غيّريه إلى رابط حسابك (مثلاً `https://instagram.com/your_username`).

### 3. نموذج التواصل (هل تصل الرسائل؟)
- حالياً النموذج يعرض رسالة «شكراً لتواصلك» فقط **ولا يرسل المحتوى إلى بريد أو خدمة**.
- لاستقبال رسائل الزوار اختر أحد الخيارات:
  - **Formspree:** تسجيل مجاني ووضع `action="https://formspree.io/f/YOUR_ID"` في نموذج التواصل ثم إرسال البيانات إلى إيميلك.
  - **Netlify Forms / خدمة استضافة:** إذا رفعتي الموقع على Netlify يمكن استخدام نماذجهم المجانية.
  - **mailto:** حل بسيط (فتح بريد العميل) بإضافة `action="mailto:your@email.com"` في النموذج — أقل احترافية لكن يعمل بدون خادم.

### 4. المحتوى والوسائط
- تأكدي أن الصور موجودة في المجلد: `1.png`, `2.png`, `3.png`, `4.png` (غلاف الكتب)، `healthymama.jpg` (الهيرو)، وأي صور أخرى مستخدمة.
- إذا كان الفيديو في «من أنا» من رابط خارجي، تأكدي أن الرابط يعمل.

### 5. (اختياري) تسجيل المبيعات في لوحة التحكم
- المدفوعات تصل إلى حساب الباي بال، لكن **لوحة التحكم لا تتحدث تلقائياً** لأنها تعتمد على بيانات محلية (localStorage) ولا يوجد خادم.
- لتسجيل كل عملية شراء في اللوحة تلقائياً تحتاجين لاحقاً إلى خادم (backend) أو خدمة تستقبل إشعارات الباي بال (IPN/Webhooks) وتخزّن المبيعات.

---

## 📋 ملخص سريع
| البند              | الحالة   | الإجراء |
|--------------------|----------|---------|
| إيميل الباي بال    | غير مضبوط | تعديل `payment-config.js` |
| واتساب / إنستغرام | روابط تجريبية | تعديل الروابط في `index.html` |
| نموذج التواصل      | لا يرسل للإيميل | ربط Formspree أو mailto أو Netlify Forms |
| الصور والملفات    | تحقق يدوي | التأكد من وجود الملفات في المجلد |

بعد إكمال البنود أعلاه يمكن اعتبار الموقع **جاهزاً للإطلاق** من ناحية الوظائف الأساسية (الشراء، التواصل، الروابط).
